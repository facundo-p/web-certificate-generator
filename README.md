# How to configure environment and run the proyect

* El proyecto está dockerizado. Para instalar los contenedores y correr la aplicación, ejecutar el comando desde la raiz del proyecto

`docker compose up -d --build`


* Open your browser and go to:
http://127.0.0.1:5050/
